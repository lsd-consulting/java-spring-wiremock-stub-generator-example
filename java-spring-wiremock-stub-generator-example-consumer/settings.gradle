pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
}

rootProject.name = 'java-spring-wiremock-stub-generator-example-consumer'

include 'api'
include 'application'
