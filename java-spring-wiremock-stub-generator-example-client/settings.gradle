pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
    }
}

rootProject.name = 'java-spring-wiremock-stub-generator-example-client'

include 'api'
include 'application'
